---
ACCESS_TOKEN_EXPIRES_IN: 1h
REFRESH_TOKEN_EXPIRES_IN: 7d
JWT_ACCESS_TOKEN_SECRET: "----- YOUR KEY -----"
JWT_REFRESH_TOKEN_SECRET: "----- YOUR KEY -----"

# Redis Configuration for Production (uses .env)
redis:
  url: ${REDIS_URL}
  db: 0
  connectTimeout: 10000
  keepAlive: true

